[使用FXLMS算法的matlab实现](https://github.com/markostam/active-noise-cancellation)

这个是模拟FxNLMS 算法的实现方式，通过运行中的自适应调整滤波实现主动降噪，这个方式问题在于：模型是现学现卖的，只能学“线性”模型，对次级路径建模误差极度敏感，对非平稳信号/突变噪声无力，参考信号与误差信号必须“高度相干”等等等，说明我们不能参考该方法但是我们可以参考他对声音的处理方式

| 代码                             | 声音世界里在做什么                                                                                                                                             |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `xp = conv(Szh,x);`            | **“预失真”**：把原始噪音 `x` 先扔进“扬声器→耳朵”的模型 `Szh`，得到 `xp`。  <br>👉 相当于 **提前知道“这坨噪音经过扬声器后会变成啥样”**。                                                              |
| `Wz = zeros(M,1);`             | 大脑清零：滤波器系数全设 0，**从头学起**。                                                                                                                              |
| `xvec = x(n:-1:n-M+1);`        | 把 **最近 M 个原始噪音样本**排成向量，准备喂给滤波器。                                                                                                                       |
| `xpvec = xp(n:-1:n-M+1);`      | 把 **同样位置的 M 个“预失真”样本**排成向量，**准备用来更新大脑**。                                                                                                              |
| `e(n) = d(n) - Wz'*xvec;`      | **算误差**：<br>① 用当前大脑 `Wz` 对原始噪音 `xvec` 做滤波 → 得到耳机**实际发出的反相声波**；<br>② 用期望信号 `d(n)` 减去它 → 得到**耳朵那里还剩多少噪音**。 <br>👉 **希望 e(n) → 0**。                      |
| `Wz = Wz + mu * xpvec * e(n);` | **只走一步“预失真方向”**：<br>用**预失真后的信号 xpvec** 乘以误差，再迈一小步（mu 控制步长）去更新大脑 `Wz`。 <br>👉 为什么用 `xpvec` 而不是 `xvec`？因为 **扬声器会把你的更新也扭曲掉**，所以你必须“提前扭”好，才能抵消掉这条扭曲路的副作用。 |

